# API base baked into the production (Pages) build. Change this one line if the Worker URL
# changes (e.g. custom domain or a project rename) — no code edits needed.
VITE_API_BASE=https://estimated-platform.robarthur1.workers.dev/api/v1
